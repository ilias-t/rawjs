/* @flow
 *
 * Singly Linked List
 */

import Node from '../Node'

class SinglyLinkedList {
  constructor(seedList: mixed[]) {
    // iterate through seed list and build the initial linked list
  }

}

export default SinglyLinkedList
